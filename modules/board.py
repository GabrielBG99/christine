from typing import List, Dict, Any
from nltk.corpus import stopwords
from utils.parameter_parser import parse_param

# TODO

def _get_parameters(params:List[str]) -> Dict[str, str]:
    response = {}
    return response


def control(params:List[str]) -> str:
    params = _get_parameters(
        params=params
    )
    return params
